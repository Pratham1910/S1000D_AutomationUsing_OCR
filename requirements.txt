# ── Core image / document processing ─────────────────────────────────────────
pillow>=10.0.0
numpy>=1.26.0
pymupdf>=1.24.0          # fitz — PDF text extraction & rendering
python-docx>=0.8.11      # .docx reading
opencv-python>=4.8.0     # image preprocessing

# ── ML / OCR backend ─────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
sentencepiece>=0.2.0
accelerate>=0.27.0

# ── Web server (Flask UI) ─────────────────────────────────────────────────────
flask>=3.0.0

# ── HTTP / networking ─────────────────────────────────────────────────────────
requests>=2.32.0

# ── Config / utilities ───────────────────────────────────────────────────────
PyYAML>=6.0.0
python-dotenv>=1.0.0
portalocker>=2.8.0
tqdm>=4.62.0
pydantic>=2.0.0
pypdfium2>=4.20.0

# ── Optional: high-accuracy PDF extraction (OpenDataLoader) ──────────────────
# opendataloader-pdf>=0.1.0
